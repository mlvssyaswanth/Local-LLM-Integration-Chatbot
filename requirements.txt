# Recipe Chatbot — Local LLM Integration
# Python 3.10+

# API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Local LLM (Ollama)
ollama>=0.1.6

# Chatbot clients
httpx>=0.26.0

# Fine-tuning (optional: transformers, peft, datasets, accelerate, bitsandbytes)
transformers>=4.36.0
peft>=0.7.0
datasets>=2.16.0
accelerate>=0.25.0
bitsandbytes>=0.41.0
